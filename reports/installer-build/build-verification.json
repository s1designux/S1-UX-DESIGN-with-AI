{
  "sourceFile": "plugins/figma-vars-installer/src/build-components.ts",
  "sourceHash": "sha256:8dd2d48f0a420ef166f5f0e96acd0f4175656ad5b73b37834b2f09b3c128f89a",
  "verifiedBy": "component-verifier",
  "verdict": "pass",
  "changeType": "structural",
  "verifiedAt": "2026-06-24T07:52:16.495Z",
  "notes": "Re-verify after layout-pass rewrite + GNB order swap + (undocumented sibling) Pagination icon-size sync. Adversarial code-level verify PASS. (1) LAYOUT PASS (most important): rewrote name-match shift to Y-band(centerY) subdivision mirroring wrapCategoryInSection. Per-member seed(footprint-name) bbox -> sort by top -> gap-midpoint band bounds(first lo=-Inf,last hi=+Inf so edge nodes captured) -> SNAPSHOT all kids into nodesByName BEFORE any move(no contamination) -> reposition in members(display) order, each footprint(set+Spec L/D+floating label) shifted together by single dy (relative pos preserved). Unit-tested: Form Control element-first build vs display order = 0 dual-membership, 0 orphan; floating labels(no footprint name) land in own member band(core bug fix). set+dark spec move with set. Cross-category isolation HOLDS: layout pass runs inside per-cat loop, kids=page.children.filter(type!==SECTION) excludes prior categories already reparented into sections (single-parent semantics, same assumption wrapCategoryInSection uses). mock guard Array.isArray(page.children)=no-op + absBBox null guard = keycheck/anatomy/render safe(no crash). (2) GNB SWAP: bar at barTop=originY(top); bottomY accumulates bar light(decorateSetFlat) then bar dark(buildSpec at +80); then menuOpts.originY=bottomY+80 reassigned BEFORE decorateSetGrouped(menuSet)+buildGroupedSpec -> decorateSetGrouped line357 sets set.x/set.y=originY so menuSet node + floating labels(floatingEmit(opts.originY)) + dark spec all move below bar. returned set=barSet('GNB') unchanged, bottomY includes menu dark. footprint('GNB')=[GNB,GNB Menu,+specs] so layout-pass member 'GNB' band covers bar+menu, moves together. (3) PAGINATION (NOT in prompt scope - flagged): makeIconInstance gains opts{wrap,keepName}; Edge component preloaded first, ICON_PX=Edge native height applied to Arrow chevron(180 rot, wrap:false) + Edge inst; centerIconInBox uses measured absBBox(pivot-independent). Variant set 3+3+3=9 + BUILT_COMPS unchanged. icon-vector-allow markers intact(Gate12 pass). (4) DISPLAY ORDER via render.js: Form Control=Input,Search,Text Area,Select Box,Dropdown,Dropdown List; GNB=GNB,GNB Utility Icon,Language Icon; all 33 sets unchanged order. Select Box=20 variants, Filter Chip=60 variants (render badge; structurally unchanged - builders untouched by diff). (5) 33 sets build clean 0 warn/err via render.js. buildOrderFor/BUILD_DEPENDENCIES untouched by diff. Gates 1-12,14,15 PASS. tsc 3 errors(flat lib-target line3474 + 2 implicit-any line3593) PRE-EXISTING at HEAD, 0 new from changed regions(968-996,2080-2128,2315-2432,3626-3672); esbuild prod bundle OK 164839 bytes. RISKS: WARN(latent) gap-midpoint algorithm has no guard if two members' full footprints vertically OVERLAP (would mis-assign far-below dark spec to next band) - but build pass enforces 140px gaps so footprints disjoint by construction, same disjoint-band assumption wrapCategoryInSection relies on; not triggerable in current build. NOT verified by me: actual Figma canvas render (layout dy packing, GNB visual stacking, pagination icon size match) - code-level only, no visual."
}
