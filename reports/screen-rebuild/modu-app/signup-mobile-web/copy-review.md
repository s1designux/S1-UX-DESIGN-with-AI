# 문구 검토 목록 — 모바일 회원가입 (river 검토용)

> **왜 이 문서가 있나:** river 가 웹쪽 문구를 검토해 정본에 반영하고, **그 반영된 문구를 앱에 그대로 사용**하기로 했다(2026-08-24). 이 문서는 그 검토를 한 번에 할 수 있게 레거시 웹 9개 화면의 전 문구를 모은 것이다.
>
> **웹·앱은 하이브리드로 완전 동일해야 한다.** 앱은 웹 화면을 끌어다 쓰므로 문구가 갈리면 안 된다. 아래 ⚠️ 표시는 지금 갈려 있는 것이다.
>
> 원본 근거: `1-inventory.md` · 레거시 웹 `aLxh458o9BRXklRFM3YYzk` / `32443:31901` · 레거시 앱 `/30812:3214`

---

## ✅ 확정된 결정 (river · 2026-08-24)

**빌드 기준은 아래 확정 문구다.** 레거시 원문이 아니다. 웹·앱 모두 동일하게 적용한다.

| # | 자리 | 확정 |
|---|---|---|
| 1 | 비밀번호 입력 헬퍼 | **`영문, 숫자, 특수문자를 조합하여 8~15자로 입력해 주세요.`** (웹·앱 공통) |
| 2 | 이메일 입력 헬퍼 | **삭제** — 잘못 들어간 내용이라 필요 없음 (웹·앱 모두 제거) |
| 3 | 도메인 표기 | **`직접 입력`** (띄어쓰기 있는 쪽으로 통일 — 콤보박스·목록 둘 다) |
| 4 | 약관 3개 전부 `(필수)` | **의도된 것** — 변경 없음 |
| 5 | 비밀번호 조건 3번째 불릿 | **`• 3자 이상 연속된 숫자·문자 사용(예: 123, abc) 불가`** (A안 · `예 :` → `예:` 공백 정정 포함) |

---

## 🚨 원래 어긋나 있던 문구 3건 (위에서 해소됨)

### ① 비밀번호 자릿수 — **웹 안에서도 모순**

| 위치 | 현재 문구 |
|---|---|
| 웹 화면5 입력 헬퍼 | `영문, 숫자, 특수문자를 조합하여 **8자 이상** 입력해 주세요.` |
| 웹 화면6 조건 다이얼로그 | `• 영문(대소문자 구분), 숫자, 특수문자 조합하여 **8~15자**` |
| 앱 비밀번호 헬퍼 | `영문, 숫자, 특수문자를 조합하여 **8~15자** 입력해 주세요.` |

같은 웹 흐름 안에서 헬퍼와 조건 안내가 다릅니다. 앱은 `8~15자`로 통일돼 있습니다.

**→ 어느 쪽이 맞나요?** (`8~15자`가 맞다면 웹 화면5 헬퍼만 고치면 됩니다)

### ② 이메일 헬퍼

| 위치 | 현재 문구 |
|---|---|
| 웹 | `입력하신 이메일로 **비밀번호를** 찾을 수 있습니다.` |
| 앱 | `입력하신 이메일로 **ID 또는 비밀번호를** 찾을 수 있습니다.` |

**→ 어느 쪽으로 통일할까요?**

### ③ `직접입력` / `직접 입력` — 띄어쓰기

| 위치 | 현재 문구 |
|---|---|
| 웹 화면7 도메인 콤보박스 | `직접입력` (붙임) |
| 웹 화면8 도메인 목록 | `직접 입력` (띄움) |

**→ 제 판단으로는 `직접 입력`(띄움)이 맞습니다.** 다르게 보시면 알려주세요.

---

## 화면별 전체 문구

### 화면 1 — 약관 동의

| 자리 | 문구 |
|---|---|
| 타이틀 | `서비스 약관에 \n동의해 주세요` |
| 전체 동의 | `전체 동의하기` |
| 항목 1 | `회원 약관 동의` + `(필수)` |
| 항목 2 | `개인정보 수집 및 이용 동의` + `(필수)` |
| 항목 3 | `만 14세 이상이에요` + `(필수)` |
| 항목 3 설명 | `만 14세 이상부터 회원가입이 가능합니다. 해당 정보는\n저장되지 않으며, 만 14세 이상 확인 용도로만 사용합니다.` |
| 하단 버튼 | `본인 인증` (공백 있음) |

> 체크 항목 3개가 전부 `(필수)` 인 것은 ✅ **의도된 것**(river 확인). 그대로 재현한다.

### 화면 2 — 약관 상세

| 자리 | 문구 |
|---|---|
| 페이지 제목 | `회원 약관 동의` |
| 조항 | `제 1조(목적)` · `제 2조(약관효력 및 변경)` |

> 약관 전문은 서비스마다 다르므로 **패턴에서는 구조만** 재현합니다. 조항 본문은 검토 대상이 아닙니다.
> 원본에 **제1조·제2조 2개만** 있습니다 — 3조 이후가 잘린 것인지 확인 필요.

### 화면 3 — 아이디 입력

| 자리 | 문구 |
|---|---|
| 타이틀 | `아이디를\n입력해 주세요` |
| 헬퍼 | `영어 소문자, 숫자를 조합하여 4~12자 입력해 주세요.` |
| 하단 버튼 | `확인` |
| 입력 예시값 | `s1design` (샘플) |

### 화면 4 — 기존 아이디 선택 (바텀시트)

| 자리 | 문구 |
|---|---|
| 시트 제목 | `기존 아이디를 사용하시겠어요?` |
| 시트 본문 | `홈페이지 또는 웹뷰어에 입력하신 정보로 가입된 아이디가 있어요. 기존 아이디를 사용하고 싶으시면 아래에서 선택해 주세요.` |
| 목록 | `s1secom1` · `s1secom2` · `s1secom3` · `s1secom4(이미 사용중인 아이디)` (샘플) |
| 버튼 (좌) | `새 아이디 사용` |
| 버튼 (우) | `아이디 선택` |

### 화면 5 — 비밀번호 입력

| 자리 | 문구 |
|---|---|
| 타이틀 | `비밀번호를\n입력해 주세요` |
| 필드1 라벨 | `비밀번호` |
| 필드1 우측 링크 | `비밀번호 조건` (밑줄) |
| 필드1 헬퍼 | ✅ **확정** `영문, 숫자, 특수문자를 조합하여 8~15자로 입력해 주세요.` |
| 필드2 라벨 | `비밀번호 확인` |
| 필드2 플레이스홀더 | `입력해 주세요` |
| 하단 버튼 | `확인` |

### 화면 6 — 비밀번호 조건 (다이얼로그)

| 자리 | 문구 |
|---|---|
| 제목 | `비밀번호 조건` |
| 본문 | `• 영문(대소문자 구분), 숫자, 특수문자 조합하여 8~15자`<br>`• 특수문자 !@#$%^&* 사용 가능`<br>✅ **확정** `• 3자 이상 연속된 숫자·문자 사용(예: 123, abc) 불가`<br>`• 생일, 전화번호, 아이디 사용 미권장` |
| 버튼 | `확인` |

> 3번째 불릿은 원본이 `• 숫자 3자 이상 연속 사용(예 : 123, abc) 불가` 였다. `abc` 가 숫자가 아니라 규칙과 예시가 어긋났다.
> ✅ **확정(A안, river):** `• 3자 이상 연속된 숫자·문자 사용(예: 123, abc) 불가` — 예시 둘을 다 살리고 규칙과 맞춘다. 콜론 앞 공백도 정정.

### 화면 7 — 이메일 입력

| 자리 | 문구 |
|---|---|
| 타이틀 | `이메일을\n입력해 주세요` |
| 도메인 콤보박스 | ✅ **확정** `직접 입력` (띄어쓰기) |
| 도메인 직접입력 플레이스홀더 | `도메인을 입력해 주세요.` |
| 헬퍼 | ✅ **확정: 삭제** — 잘못 들어간 내용 (웹·앱 모두 제거) |
| 하단 버튼 | `확인` |

### 화면 8 — 이메일 도메인 선택 (바텀시트)

| 자리 | 문구 |
|---|---|
| 시트 제목 | `이메일` |
| 목록 | `naver.com` · `kakao.com` · `gmail.com` · `nate.com` · ✅ `직접 입력` |

### 화면 9 — 완료 (웹 = 앱 설치 안내)

| 자리 | 문구 |
|---|---|
| 안내 1 | `홍길동님, 환영합니다!` (파란색 · 샘플 이름) |
| 안내 2 | `서비스를 이용하시려면\n앱을 설치해 주세요` |
| 버튼 (좌) | `다음에 설치` |
| 버튼 (우) | `앱 설치` |
| 앱 아이콘 자리 | `앱 아이콘` (플레이스홀더 라벨) |

> **웹은 앱 설치 안내, 앱은 회원가입 완료 화면**으로 갈립니다(river 확인). 앱쪽 완료 화면 문구는 앱 경로 조사 때 확인합니다.

---

## 샘플 값으로 남길 것 (검토 불필요)

실제 문구가 아니라 예시입니다. 패턴에서는 샘플 표기로 유지합니다.

- `홍길동님` — 사용자 이름 자리
- `s1secom1` ~ `s1secom4` — 아이디 목록 자리
- `s1design` — 아이디 입력 예시
- `앱 아이콘` — 이미지 자리표시자
- `m.s1.co.kr` — 브라우저 주소 (StatusBar Web variant 가 처리)

---

## 앱바 제목 (헤더 컴포넌트 작업에서 다룸)

| 화면 | 현재 |
|---|---|
| 화면 1·3·5·7·9 | `회원가입` — **흰 배경에 흰 글자라 안 보임** |
| 화면 2 | `제목` — 오버라이드 안 된 기본값 |

river 확인: **안 보이는 게 맞다.** 다만 현재 모바일 코어에 **헤더 컴포넌트가 없어** 신설이 필요하고, **타이틀 없는 유형**도 함께 추가한다. → 별도 세션에서 진행.

---

## 검토 후 할 일

1. ✅ river 확정 완료 (2026-08-24) — 위 「확정된 결정」 표 참조.
2. 확정 문구를 **정본에 반영**한다. (헤더 컴포넌트 작업 이후)
3. 패턴 빌드는 그 확정 문구로 진행한다 — **레거시 원문이 아니라 확정 문구가 기준이다.**
4. 앱 경로는 같은 확정 문구를 그대로 쓴다(완료 화면만 다름).
