# 정본 승인 감사 — 2026-09-07 (river 결정 포함)

> **무엇인가:** 정본 3파일(`vars-data.ts` · `textstyles-data.ts` · `build-components.ts`)에 river 승인 없이 들어간 항목이 있는지 전수 감사한 결과와, 그에 대한 river 결정 5건의 기록.
> **읽기 전용 감사였다** — 감사 과정에서 정본·파생을 한 글자도 고치지 않았다.
> 결정 화면(아티팩트): `https://claude.ai/code/artifact/abaca112-c241-43a4-bd9d-c0f80d745c85`
> 결정 원본은 그 화면의 db `decisions/hd1`~`hd5` 에 river 가 직접 누른 값으로 저장돼 있다(2026-09-07 07:43~07:47Z).

---

## 1. 어떻게 봤나 (방법 · 재현 가능)

| 단계 | 방법 | 결과 |
|---|---|---|
| 이름 신설 이력 | 정본 3파일을 건드린 **커밋 151개**를 실제로 체크아웃해, 각 시점의 항목 이름 전집합을 뽑아 연속 비교. 추출 로직은 **Gate 34 `canon-addition-check.js` 의 `currentItems()` 그대로**(새 판정 기준 만들지 않음) | 신설 이름 **239건** |
| variant 축 이력 | Gate 34 가 일부러 제외하는 축값은 **`variant-coverage-check.js` 의 `installerAxisMatrix()`** 방식으로 별도 추출(빌더를 가짜 Figma 로 실행) · 2026-07-01 이후 커밋 38개 | 축값 **265개**, 변동 19커밋 |
| 승인 인용문 검증 | 승인 원장 16건의 `quote` 를 세션 기록 **102개 파일 · 사람 발화 758건**(`origin.kind=human`)과 직접 대조 | **위조 0건** |
| 철회 범위 검증 | `476f1cd`(focus-visible 17건 철회)의 파생 모델을 노드 단위로 전후 비교 | 사라진 노드 **124개 전부 focus ring** — 옆 코드 손실 0 |
| 자백성 주석 전수 | `ui-library/src/**` 에서 "정본에 없" · `notInCanon` · "오케스트레이터가 정한" 전수 | 16개 컴포넌트에서 수집 |

**추출기 검증:** HEAD 에서 뽑은 617건 = `canon-additions-baseline.json` 의 617건과 정확히 일치.

**확인 못 한 것(정직 표기):**
- **2026-08-11 · 08-20 등 일부 날짜의 세션 기록이 이 컴퓨터에 없다.** 그 날짜 항목의 "근거 없음"은 *승인이 없었다*가 아니라 **확인할 수 없다**이다.
- 2026-05~06월 커밋 16개는 당시 파일 구조가 달라(정본 파일 자체가 없던 시기) 자동 비교에서 제외.
- variant 축은 2026-07-01 이후만 훑었다(그 이전은 Gate 34 가 없던 시기).
- 이 감사는 **⭐ 총괄 자가 수행**이다. 🤖 독립 검증 에이전트는 돌리지 않았다.

---

## 2. river 결정 5건

| # | 항목 | 결정 | 뜻 |
|---|---|---|---|
| HD-1 | 라인탭 PC XSM(높이 40 · 글자 14) | **A · 유지** | 2026-09-07 river 승인으로 정본 기준 확정. 근거는 이 문서다(2026-08-11 승인 주장은 확인 불가로 폐기) |
| HD-2 | NavBar `Platform=App + Keyboard` · `Web + Keyboard` | **A · 유지 (결정 완료 2026-09-07)** | river 가 직접 정리한 것으로 확인. 근거·후속은 §7 |
| HD-3 | 드롭다운 패널 최대 폭 320px | **상한 폐기 — 목록 폭 = 트리거 폭 (2026-09-07 최종)** | 같은 날 두 번 결정했다: ①320 확정 → ②번복 "드롭다운은 상단 트리거의 크기에 따라서 동일하게 반영되되 트리거 자체가 너무 좁은 경우에는 … 너무 작아지지 않게끔". 최종 = 폭은 트리거를 따르고 하한 140px, 상한 없음, 넘치는 글자는 말줄임(…). 기록 = workflow-state D22(폐기)·D23(확정)·D24(필터칩은 하한 없이 칩 폭과 완전 동일 — 실제 칩 74·103px 가 전부 하한 140 에 걸려 규칙이 무력화돼 있었다) |
| HD-4 | 웹 상태 이름 74건 | **B · 어긋난 9건만 따로 결정** | 나머지 65건은 인정. 9건은 §4 에 정리 — river 결정 대기 |
| HD-5 | ⭐ 가 정한 동작·수치(메커니즘) | **A · 승인 대상으로 올린다** | 앞으로 이런 항목은 needs-decision 으로 올린다. 현존 목록은 §3 |

---

## 3. HD-5 적용 — 지금 승인 대기로 올라오는 「⭐ 가 정한 것」 목록

정본에 규정이 없어 ⭐ 가 값·동작을 정한 항목 전수(자백성 주석 기준). **HD-5 결정에 따라 이제부터 river 승인 대상이다.**

| # | 컴포넌트 | 무엇을 ⭐ 가 정했나 | 상태 |
|---|---|---|---|
| M-1 | time-picker | 휠 스크롤 메커니즘 — 행 높이 44px · 창 272px · 위아래 여백 114px | **이미 문제로 드러남** — river 제보(2026-09-07 "간격이 다름")로 원본과 어긋난 것이 확인돼, 다른 세션이 현재 정본에 열 폭을 못 박는 중 |
| M-2 | time-picker | 휠 모드 apply 버튼 상시 활성(목록 모드와 다름) | **승인 완료 A(2026-09-08)** — 지금대로 유지. river 메모: 목록 모드는 시·분을 고른 뒤 확인 활성 |
| M-3 | time-picker | 목록 세로 스크롤 방식(정본은 clip 만 규정) | **승인 완료 A(2026-09-08)** — 지금대로 유지 |
| M-4 | date-picker | 달력을 트리거 아래 **8px** 띄움 · 아래 공간 부족 시 위로 뒤집기 | **승인 완료 A(2026-09-08)** — 지금대로 유지 |
| M-5 | date-picker | 화면별 이동 단위(날짜=한 달 · 월=1년 · 연=12년) | **승인 완료 A(2026-09-08)** — 지금대로 유지 |
| M-6 | date-picker | 격자 키보드 조작(화살표·Home/End·PgUp/PgDn·Enter·Esc) | **승인 완료 A(2026-09-08)** — 지금대로 유지 |
| M-7 | date-picker | 선택 셀 hover 토큰을 시각 빌더에 없는데 웹이 사용 | **승인 완료 A(2026-09-08)** — 웹이 맞다. 정본 배선은 후속(§9 참조) |
| M-8 | date-picker | 값 입출력 형식 ISO(YYYY-MM-DD), 표시 YY.MM.DD | **결정 후 원복(2026-09-08)** — B 로 4자리 시행 → 같은 날 river 지시로 `YY.MM.DD` 로 되돌림. 최종 = 지금까지와 동일 |
| M-9 | date-picker | 기간 트리거 문구 `YY.MM.DD - YY.MM.DD` — **⭐ 가 "승인 대상 아님"이라고 스스로 판정한 항목** | **결정 B(2026-09-08)** — 구분자를 ` ~ ` 로 변경, 웹 반영 완료 |
| M-10 | select | Open×Filled 교차 시 Open 시각 우선(정본에 교차 셀 없음) | **미응답(2026-09-08)** — 지금대로(열림 우선) 유지, 코드 변경 0건 |
| M-11 | dropdown | 패널 최대 폭 320px | **해소 — 상한 자체가 폐기됨(river 2026-09-07 D23)** |

> M-9 는 HD-5 의 직접 계기다 — 무엇이 승인 대상인지를 ⭐ 가 스스로 정하고 있었다.

---

## 4. HD-4-B 적용 — 정본과 이름·짜임이 어긋난 웹 상태 9건 (river 결정 대기)

웹 상태 이름 74건 중 65건은 정본 상태를 그대로 옮긴 것으로 확인돼 **인정**. 아래 9건만 남는다.

| # | 컴포넌트 | 정본(Figma) | 웹 배포본 | 무엇을 정해야 하나 |
|---|---|---|---|---|
| N-1 | 입력칸 | `editing` | `focus` | 같은 모습을 다르게 부른다. 어느 이름으로 통일할까 |
| N-2 | 체크박스 | `dis+checked` | `disabledChecked` | 표기법만 다름(붙임표 vs 낙타표기). 규칙을 정할까 |
| N-3 | 라디오 | `dis+selected` | `disabledSelected` | 위와 같음 |
| N-4 | 토글 | `disabled` + `on` (두 축) | `disabledOn` (한 이름) | 축을 합쳐 부르는 방식을 인정할까 |
| N-5 | 드롭다운 | `selected` | `selectedText` | 유형별로 쪼갠 이름 |
| N-6 | 드롭다운 | `selected` | `selectedCheckbox` | 위와 같음 |
| N-7 | 모달 | 상태 축 없음 | `open` | 웹에만 필요한 상태(열림) — 정본에 없어도 되는가 |
| N-8 | 모달 | 상태 축 없음 | `closed` | 위와 같음 |
| N-9 | 달력 | 상태 축에 없음 | `hover` | **이미 승인됨**(2026-09-04 river, 인용문 검증 완료) — 대조표에는 남기되 결정 불필요 |

권장: N-2·N-3·N-4 는 **표기 규칙 1건**으로 묶어 한 번에 정하면 된다(9건이 아니라 실질 4가지 질문: 이름 통일 · 표기법 규칙 · 유형별 분화 허용 · 웹 전용 상태 허용).

---

## 5. 후속 작업 (별도 세션)

- **HD-2 · HD-3**: 각각 별도 세션 프롬프트로 인계(이 문서 작성 시점에 river 에게 전달).
- **HD-4**: §4 의 4가지 질문으로 결정 화면을 다시 만들지, 문서로 답할지는 river 선택.
- **HD-1 후속(미완):** `build-components.ts:1832` 주석의 *"PC XSM 은 river 승인 신규 기준(2026-08-11)"* 은 확인 불가한 출처다. **2026-09-07 승인**으로 고쳐 적어야 하는데, 이 세션에서는 **하지 않았다** — 같은 작업트리의 다른 세션이 그 파일을 미커밋 상태로 수정 중이라(Gate 13 이 내 편집 이전부터 stale) 손대면 남의 작업을 함께 인증하게 된다. 그 세션의 작업이 커밋된 뒤 **주석 1줄 기계적 수정 + `--by orchestrator --change mechanical` 자가인증**으로 처리할 것.

---

## 6. 이번 감사로 확인된 재발 패턴

`reports/repeated-requests.json` 의 `approval-claimed-in-comment-only` 로 등록(count 4).
승인이 **코드 주석·작업 기록의 자기 진술로만** 남으면, 나중에 아무도 그것이 실제 승인이었는지 되짚을 수 없다 —
Line Tab XSM · NavBar 키보드 유형 · 드롭다운 320px · 타임피커 휠 수치가 모두 같은 형태였다.

---

## 7. HD-2 결정 — NavBar 키보드 변형 (river, 2026-09-07)

### 결정: **유지**. 그리고 정본 코드가 낡은 것이 함께 드러났다.

감사 시점에는 "승인 기록 없음"이었으나, **river 가 이 변형을 직접 정리했다고 확인**했다.
따라서 「근거 없이 들어온 항목」이 아니라 **기록만 누락된 항목**이었다.

> river 발화(2026-09-07):
> "navbar는 내가 직접 정리했는데"
> "원래는 웹화면에서는 브라우저 도구모음이 있는것으로 정리했었는데, 실제 사용화면을 보니 키보드가
> 올라오는 상황에서는 브라우저 도구모음이 표출이 안되더라고. 그래서 동일한 모양으로 통합을 했어.
> 배리언츠를 줄이고 통합하기에는 웹 앱에서의 분리가 필요한 경우도 있어서"

### 확정 사항

| 항목 | 확정 |
|---|---|
| 두 변형 존치 | **유지** — App·웹의 키보드 모양이 같아졌지만 **변형은 합치지 않는다**. 웹/앱 구분이 필요한 경우가 있다 |
| `Web + Keyboard` 구조 | 키보드 296 + 하단 내비 45 = **360×341**, **브라우저 도구모음 없음** |
| 근거 | 실제 사용 화면에서 **키보드가 올라오면 브라우저 도구모음이 표출되지 않는다**(river 실사용 관찰) |
| 도구모음이 남는 곳 | 키보드가 없는 기본 `Platform=Web`(360×95) 은 **그대로 유지** |

### 왜 쓰이는 곳 조사가 삭제안을 배제했나 (참고)

살아있는 소비처 5곳 — 로그인 패턴 입력 A·B(`App + Keyboard`), 회원가입 모바일웹 인스턴스 3개
(`Web + Keyboard`), 패턴 규칙 문서 4곳, 설치기 패턴 탭 `pattern-data.ts`(river 지시 2026-09-02).
레거시 원본(`8403:60155`·`30812:3889`)에도 키보드가 실재한다 — 지어낸 모양이 아니다.

**모바일 헤더 D5 와 충돌하지 않는다:** D5 는 *웹 배포본*에 OS 크롬을 넣지 말라는 결정이고,
D6 가 *시안*에서는 그림으로 두는 것을 명시 허용한다. NavBar 는 웹 배포본에 아예 없는
Figma 시안 전용 크롬이며, 기본형부터 이미 OS 크롬(하단 내비·주소창, river 결정 2026-06-25)이다.

### 남은 조치 — 정본 코드가 Figma 를 따라오지 못했다

`Web + Keyboard` 가 세 곳에서 갈라져 있다.

| 표면 | 값 |
|---|---|
| 정본 `build-components.ts:5288~5298` | **360×391** · 키보드 + **브라우저 도구모음** + 하단 내비 ← 낡음 |
| Figma 실제 컴포넌트(`2238:17258`) | **360×341** · 도구모음 없음 |
| 패턴 규칙 · `pattern-data.ts` `NAV_WEB_KB` | **360×341** · 도구모음 없음 |

2026-09-01 회원가입 작업이 Figma 에서 도구모음을 제거하고 341 로 확정했으나
(`signup-mobile-web/3-build.md:131`), **정본 코드에 되먹이지 않았다.**
설치기를 다시 돌리면 391 + 도구모음이 되살아나 회원가입 화면 3개가 조용히 깨진다.

**조치(river 승인 2026-09-07):** `buildNavBar` 의 `webKeyboard` 에서 `buildBrowserToolbarRow`
호출을 제거하고 높이 391 → 341 로 맞춘다. 기본 `Platform=Web`(95) 은 손대지 않는다.

**아직 실행하지 않았다** — 같은 작업트리의 다른 세션이 `build-components.ts` 를 미커밋
상태(time-picker 휠 열 폭, §3 M-1)로 수정 중이다. 지금 편집하면 남의 변경까지 함께
인증하게 되므로(Gate 13), **그 세션이 커밋된 뒤** 실행한다.

실행 절차: 정본 수정 → `npm run tokens:reconcile`(component-facts·guide-model 재생성)
→ 🤖 `component-verifier` 실제 spawn(구조 변경이므로 자가인증 금지, 하드룰 H1②)
→ `installer-build-verify-check.js --record --by component-verifier` → Gate 13 기록.

---

## 8. HD-4 후속 — 이름 4가지 결정과 대응표 채움 (river, 2026-09-08)

### 결정 (river 발화: "추천대로 ①A ②A ③A ④A로 가자")

| # | 질문 | 결정 |
|---|---|---|
| ① | 입력칸 `editing`(정본) ↔ `focus`(웹) | **둘 다 `focus` 로 통일** — 웹 표준 어휘(`:focus`·focused)와 잇는다 |
| ② | `read-only`(정본) ↔ `readOnly`(웹) 표기 | **층별 표기 유지 + 대응표로 잇기** — 마크업은 kebab, JSON 키는 camel 이 각 층의 표준이다 |
| ③ | 페이지네이션 바 상태와 칸 상태 | **나눠 적기** — 정본이 두 세트(Pagination · Pagination Cell)로 나눈 구조를 대응표에서도 유지 |
| ④ | 모달 `open`/`closed` | **「화면에서만 있는 표출 상태」로 표시하고 정본에는 상태 축을 만들지 않는다** |

### 실행 완료 — 대응표(`canonicalStateMap`) 채움

| 컴포넌트 | 채운 내용 |
|---|---|
| button | Default·Hover·Pressed·Disabled → 정본과 1:1 |
| input | Default·Filled·**Editing→focus**·Error·Correct·**Read-Only→readOnly**·Disabled |
| pagination | Single·First·Last·Middle + **`Pagination Cell · Selected` → selected**(출처 세트를 밝혀 분리) |
| modal | `closed` 는 매핑 대신 `notInCanon.closedState` 에 사유 기록(④ 결정) |
| date-picker | `hover` 는 `notInCanon.triggerHoverState` 에 기록(2026-09-04 승인 원장 참조) |

검증: `ui:build` 재생성 → `ui:build:check` 통과 · `ui:test` 통과 · **Gate 34 신설 0건**(상태 키 자체는 건드리지 않았다 — 대응표만 추가).

### 남은 후속 — ① 의 정본 쪽 개명

`Editing → Focus` 는 **정본 변경**이라 이 세션에서 하지 않았다.
- Figma Input 변형세트의 State 축 값 개명 → 하드룰 H1① 대상(빌드=🏗️ `figma-library-builder` / 검증=🤖 `component-verifier` 분리). **이 세션은 Figma MCP 연결이 끊겨 있어 불가.**
- `build-components.ts:908~914` 의 `{ name: "Editing" }` → `"Focus"` · 파생 재생성.
  다른 세션이 같은 파일을 미커밋 상태로 수정 중이라 대기(§7 과 같은 사유).
- 개명 전까지 대응표의 `"Editing": "focus"` 가 **현재 사실**이며, 개명 후 `"Focus": "focus"` 로 바뀐다.

### 감사 결과 정정 — HD-3 은 다른 세션에서 이미 결정됐다

이 문서 §2 는 HD-3(드롭다운 최대 폭 320px)을 「보류」로 적었으나, **같은 날 다른 세션이 river 와 결정을 마쳤다**(작업트리 `ui-library/src/components/dropdown/dropdown.css:14~17`):
**320px 상한 폐기 · 폭은 트리거 폭 기준(최소 140) · 넘치는 옵션은 줄바꿈이 아니라 말줄임(…)** — river 결정 2026-09-07("글이 넘어가는 경우 줄임말(...)표시"). 근거 조사는 `reports/ui-library/dropdown-max-width/NEEDS-DECISION.md`.
따라서 §3 의 **M-11(dropdown 최대 폭)은 해소**됐다. 아직 커밋 전이라 HEAD 에는 옛 값(320·줄바꿈)이 남아 있다.

---

## 9. HD-5 후속 — 메커니즘 승인 9건 결정 (river, 2026-09-08)

결정 화면(`reports/mechanism-approvals/decision-screen.html`, 게시본 `artifact/a585db0e…`)에서 river 가 직접 고른 값을 그대로 옮긴다. 원본은 그 화면의 `decisions` 저장소(문서 id `m2`~`m10`).

| # | 항목 | river 선택 | 메모(원문) | 처리 |
|---|---|---|---|---|
| M-2 | 휠 apply 상시 활성 | **A** | "드롭다운방식은 선택된 시간이 없기때문에 시간을 선택한 후에 확인이 활성화 되어야함." | 코드 변경 0건. manifest `wheelApplyAlwaysEnabled` 에 승인·인용 기록 |
| M-3 | 목록 세로 스크롤 | **A** | — | 코드 변경 0건. manifest `columnScroll` 기록 |
| M-4 | 팝오버 8px·뒤집기 | **A** | — | 코드 변경 0건. manifest `popoverPlacement` 기록 |
| M-5 | 화면별 이동 단위 | **A** | — | 코드 변경 0건. manifest `monthYearNavigation` 기록 |
| M-6 | 격자 키보드 조작 | **A** | — | 코드 변경 0건. manifest `keyboard` 기록 |
| M-7 | 선택 셀 hover 색 | **A** (웹이 맞다) | — | manifest `selectedHoverToken` 기록. **정본 배선 완료(2026-09-09)** — `State="Selected Hover"` 신설, 아래 「남은 후속」 1 참조 |
| M-8 | 날짜 값 형식 | **B → 원복** | "2026.09.08" → "화면에 보이는 글자를 2026.09.08로 바꿔줘" → "오케이 그럼 원래대로 YY.MM.DD방식으로 원복하고 커밋해줘" | 4자리로 바꿔 실물을 보여준 뒤 river 가 되돌리기로 결정. **최종 = `YY.MM.DD` 유지, 값은 ISO 유지 — 코드 변경 0건** |
| M-9 | 기간 트리거 문구 | **B** (물결표) | — | `date-picker.js:339-340` 구분자 `" - "` → `" ~ "`. `ui:build` 재생성·렌더 확인 완료 |
| M-10 | Open×Filled 교차 | **미응답** | — | 코드 변경 0건. 지금대로(열림 우선) 유지. `select/manifest.json` `openFilledCross` 에 "제시했으나 미응답" 을 명시(2026-09-09) — 승인도 반려도 아님을 기록에 남긴다 |

M-2 의 river 메모는 웹 현재 동작과 어긋나지 않는다 — 목록 모드는 이미 시·분을 다 골라야 confirm 이 풀리고, 메모는 그 규칙을 확인해 준 것이다. 휠 모드만 상시 활성으로 남는다.

### 남은 후속 2건

1. **M-7 정본 배선 — 완료 (2026-09-09).** `calCellCompsForSize` 의 STD 표에 `State="Selected Hover"` 를 신설하고 채움·테두리 모두 `color/date-picker/cell/bg/selected-hover` 를 물렸다(텍스트는 `text/selected` 유지 — 웹 `date-picker.css` 의 `[data-state="selected"]:hover` 규칙과 한 벌). `buildCalendarCellLayout` 의 `stdStates` 도 6열로 늘려 스펙 시트에 드러낸다. 새 토큰 0건.
   정본 구조 변경이라 **하드룰 H1②** 대상 — 🤖 `component-verifier` 실제 spawn 후 `installer-build-verify-check.js --record --by component-verifier` 로 Gate 13 기록.
   `date-picker/manifest.json` 의 `selectedHoverToken` 도 "해소" 로 갱신했다(더 이상 notInCanon 이 아니다).
   (2026-09-08 미착수 사유였던 "다른 세션의 미커밋 변경"은 2026-09-09 저장소 정리로 해소됐다.)
   함께 대기 중인 같은 파일 작업: NavBar `Web + Keyboard` 391→341 · `build-components.ts:2146` 주석 날짜 정정 · `Editing → Focus` 개명(§8).

2. **M-8 은 남은 후속이 없다** — 아래 경위대로 원래 형식으로 되돌아갔다.

### M-8 경위 — 바꿨다가 되돌린 기록 (같은 결정을 다시 꺼내지 않기 위해)

river 가 화면에서 B 를 고르고 메모에 `2026.09.08` 만 남겨, 그것이 **보이는 형식**인지 **주고받는 값 형식**인지 되물었다. river 답: "화면에 보이는 글자를 2026.09.08로 바꿔줘". 그대로 4자리(`YYYY.MM.DD`)로 바꿔 웹·안내 사이트에 반영하고 실물을 보여줬다.

그때 함께 보고한 부작용이 되돌린 이유다 — **기간 문구가 최소 폭에서 잘린다.** 4자리 기간 문구 `2026.09.04 ~ 2026.09.11` 은 약 **208px** 을 요구하는데 트리거 최소 폭은 정본 값 **180px**(`build-components.ts:3696` `trigger.resize(180)`)이다. 좁게 쓰는 자리에서 `2026.09.04 ~ 202…` 로 말줄임된다(레이아웃은 안 깨진다 — 기존 말줄임 규칙이 받는다). 단일 선택은 140px 면 충분해 영향이 없었다.

river 결정(2026-09-08): "오케이 그럼 원래대로 YY.MM.DD방식으로 원복하고 커밋해줘" → **표시는 `YY.MM.DD` 2자리로 되돌렸다.** 정본과 웹이 다시 일치하므로 이 항목에 남은 정본 작업은 없다. `registry` HD-10 도 2자리 확정으로 되돌리되, 한 번 시행했다가 되돌린 사실은 기록으로 남긴다.

M-9(구분자 ` ~ `)는 원복 대상이 아니며 그대로 유지된다.
