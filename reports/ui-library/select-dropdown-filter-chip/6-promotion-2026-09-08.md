# 6-promotion — 드롭다운 목록 폭 규칙 확정 · 말줄임 (2026-09-08)

> 대상 work-id: `select-dropdown-filter-chip` · 커밋 `a9533e3`
> 이 문서는 D22~D28 결정의 승격 근거를 한곳에 모은 것이다. 상세 경위는 `workflow-state.json` 의 decisions 참조.

## 1. 확정된 규칙 (river 최종 · 2026-09-07)

> "드롭다운은 최소 100px / 칩이 드롭다운보다 width값이 작은경우 드롭다운은 100px로 보여진다 /
> 칩의 width가 100px이상이 되는경우 드롭다운의 width는 칩의 width와 동일하게 표출된다"

① 목록 최소 폭 100px ② 트리거가 100 미만이면 목록 100px ③ 트리거가 100 이상이면 목록 = 트리거 폭
④ 최대 폭 상한 없음 ⑤ 넘치는 옵션 글자는 말줄임(…), 마우스를 올리면 전체 표시(웹 전용)

## 2. 실측 증거 (12경우 전수 일치)

| 경우 | 트리거 | 목록 | 규칙상 |
|---|---|---|---|
| 칩 60px(강제) | 60 | 100 | 100 ✅ |
| 실제 칩 PC SM·제목없음 | 80.3 | 100 | 100 ✅ |
| 실제 칩 PC MD·제목없음 | 86.3 | 100 | 100 ✅ |
| 칩 99 / 100px(경계) | 99 / 100 | 100 / 100 | ✅ |
| 실제 칩 PC SM·제목있음 | 108.5 | 108.5 | 칩과 동일 ✅ |
| 실제 칩 PC MD·제목있음 | 114.5 | 114.5 | 칩과 동일 ✅ |
| 실제 칩 Mobile MD·제목있음 | 108.5 | 108.5 | ✅ |
| 칩 160 / 320px(강제) | 160 / 320 | 160 / 320 | ✅ |
| 셀렉트 180 / 460px | 180 / 460 | 180 / 460 | ✅ |

화면: `reports/ui-library/dropdown-max-width/final-rule-check.png` · `compare-after.png` · `chip-width-options.png`
안내 페이지 실물: `screens/13-chip-width-parity-pc.png` · `15-final-rule-guide-pc.png` · `16-canon-truncation-pc.png`

## 3. 규칙이 기록된 곳 (river 지시 "정본에도 적혀 있어야 한다")

| 자리 | 무엇 |
|---|---|
| `ui-library/src/components/dropdown/dropdown.css` | 헤더 주석에 규칙 ①~⑤ 전문 + river 원문 |
| `plugins/figma-vars-installer/src/build-components.ts` | buildDropdownList·buildFilterChip 위 규칙 주석 + Figma 표현 한계 |
| `registry/components/dropdown.json` `guide.panelWidthRule` | 기계가독 사양 — 규칙·웹 구현·실측값·Figma 한계 |
| `registry/components/filter-chip.json` · `select.json` | 위 사양 참조 |
| `assets/js/ui-library-guide.js` | 안내 페이지 사용자용 설명 |

## 4. 정본(Figma 설치기)에 반영한 것 — GUI 로 보이는 항목만 (river 지시)

`buildDropdownList` 옵션 텍스트: `layoutGrow=1` · `textAutoResize="HEIGHT"` · `textTruncation="ENDING"` · `maxLines=1`.
**폭 리터럴 140 은 유지** — 정본은 트리거도 목록도 140 이라 규칙 ③에 이미 부합하고, 100 은 하한일 뿐이다.
「트리거 폭을 따라간다」는 Figma 컴포넌트가 고정 폭이라 캔버스로 표현 불가 → 규칙만 남겼다.

## 5. 검증

- 🤖 `component-verifier` 시나리오 D **PASS** (2026-09-08) — `4-verification-installer-2026-09-08.md`
  - 지문 대조로 회귀 0 증명: 바뀐 시각사양 10행 전부 Dropdown List 옵션 글자, 토큰 diff 0, 타 컴포넌트 영향 없음
  - PROP_CLASS 등록이 검사 약화가 아님을 독해+실측으로 확인
- Gate 13 기록: `structural` · `sha256:702481d5344f…` — 커밋된 정본 파일 해시와 일치 확인
- 기계검사: `installer:check` · `installer:build` · `ui:contract` · `ui:build:check` · `ui:test:check` · `ui:icons` · `ui:guide:render` · `gate:check`(게이트 50개) 전부 통과

## 6. 독립 검증 생략 범위 (waiver)

D23·D24·D25·D26(웹 배포본 CSS·JS)은 river 결정으로 독립 검증을 생략했다 — "독립 검증은 생략해. 내가 직접 볼게".
**정본 변경(D28)은 생략하지 않고 🤖 검증을 실제로 돌렸다** — Gate 13 이 기계로 요구하기 때문이다.

## 7. 아직 안 된 것 (정직 표기)

- **Figma 캔버스 실제 반영**: MCP 인증이 끊겨 이 세션에서 불가. river 가 새 설치기 zip 을 Figma 에서 실행해야 한다.
- **river 눈 검수**: ①②는 스크린샷·수치로 확인받았고, ③(마우스 올림 느낌)은 river 가 보지 않은 채 커밋을 지시했다.
- **🤖 지적 🟡-3**: `build-components.ts` 규칙 주석의 따옴표 문장은 D22+D25 를 합친 요약이라 결정 id 를 다는 편이 낫다.
  Gate 13 해시를 깨지 않으려 이번엔 손대지 않았다 — 다음 정본 편집 때 함께 처리한다.
- **보류된 남의 작업**: `reports/handoff/peer-wip-adoptInto-2026-09-08.patch` — 원저자 세션이 재개하면 알려야 한다.
