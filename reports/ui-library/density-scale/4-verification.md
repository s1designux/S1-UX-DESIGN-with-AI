# 4-verification — 밀도(density)

> 2026-09-15 갱신. 배포본 **0.5.7**. 독립 검증 5회차에 **PASS**. 누적 결함 8건 전부 해소(고친 것 6건 + river 결정으로 닫힌 것 2건).

## 기계 검사 (2026-09-15 · 전부 종료코드 0)

| 명령 | 결과 |
|---|---|
| `npm run ui:contract` | ✅ status=candidate · errors=0 |
| `npm run ui:build:check` | ✅ source → dist 재생성이 작업 트리와 byte 동일 |
| `npm run ui:test:check` | ✅ |
| `npm run ui:icons` / `ui:icons:origin` | ✅ / ✅ |
| `npm run ui:guide:render` | ✅ 컴포넌트 25종 × PC·Mobile 실제 렌더 |
| `npm run ui:version` | ✅ 0.5.7 · 부품 25종 전부 정본과 일치 |
| `npm run ui:density` | ✅ 규칙 132줄 · 경고 7건(대체 크기 3 · 기본높이 1 · 모바일 크기 없음 3) |
| `npm run ui:state` | ✅ |
| `npm run gate:check` | ✅ 오류 0 · 경고 17 (`board:refresh` 후) |

## 브라우저 실측 — 한 줄에 놓았을 때 높이 (px)

검수 화면 `density-fixture.html`, 컴포넌트 10종 전부.

| | 넓게 44 | 보통 34 | 좁게 28 | 모바일 48 |
|---|---|---|---|---|
| 버튼 | 44 | 34 | 28 | 48 |
| 입력창 | 44 | 34 | 28 | 48 |
| 선택 | 44 | 34 | 28 | 48 |
| 날짜 선택 | 44 | 34 | 28 | 48 |
| 시간 선택 | 44 | 34 | 28 | 48 |
| 칩 | 34 \* | 34 | 28 | 30 \*\* |
| 필터 칩 | 34 \* | 34 | 28 | 30 \*\* |
| 멀티 토글 | 44 | 34 | **34** ❌ | **19** ❌ |
| 표(칸) | 44 | 34 | **34** ❌ | 44 |
| 드롭다운(줄) | 44 | 34 | 28 | **27** ❌ |

\* 칩 계열은 44 크기가 없어 가장 가까운 아래(34)로 대신한다 — river 승인 2026-09-14 "칩이 34로 내려가는 거 괜찮아".
\*\* 칩 계열의 모바일 높이는 정본이 30 이다.

**예외가 이기는지** — `narrow` 안에서 `data-size="md"` 를 직접 준 버튼 44, 안 준 버튼 28. ✅
**겹침** — 컴포넌트가 자기 `data-break` 를 달고 있어도 결과가 같다(넓게 칸 = 겹침 칸, 모바일 칸 = 겹침 칸). ✅

**감싸기 안에서 크기만 직접 줬을 때** — 화면 구분을 안 적고 `data-size` 만 줘도 10종 전부 그 크기로 선다(28·28·28·28·28·28·28·34·34·28). ✅
**모바일 터치 영역** — 입력의 지우기 버튼이 모바일 48×48 · PC 28×28. ✅

## 1차 독립 검증 (🤖 component-verifier · 시나리오 F · 2026-09-15 · 0.5.4) — **FAIL**

❌(a) 5건. 그중 **결정이 필요 없는 3건은 고쳤다**.

| # | 무엇이 | 조치 |
|---|---|---|
| A | 컴포넌트가 자기 `data-break` 를 달고 있으면 밀도 규칙이 빗나가 높이가 아예 안 잡힘(입력창 17.5px 등). 배포되는 예시 마크업 6종이 바로 그 상태 | **고침** — `[data-break="pc"]` → `:not([data-break="mobile"])` 로 옮겨 감싸기 선언이 닿게 했다. 반대쪽을 직접 적어 둔 것은 그대로 이긴다 |
| B | 날짜 선택이 감싸기의 화면 선언을 안 읽어, 모바일 선언만 하면 높이는 48 인데 PC 팝업이 열림 | **고침** — 자기 `data-break` 가 없으면 조상 `data-s1-break` 를 따른다 |
| C | 표가 넓게에서 38 로 내려앉음(기본 규칙의 44 를 못 읽고 한 단계 아래를 깔아 덮어씀) | **고침** — 크기 규칙이 높이를 주는 **같은 자리**의 기본 규칙에서 기본 높이를 읽고, 그 높이가 이미 나오는 단계에는 규칙을 깔지 않는다. 표 넓게 44 복구 |
| D | 좁게에서 멀티 토글·표가 34 로 **올라감** — 승인 문구는 "가장 가까운 아래"뿐 | **river 결정 대기** |
| E | 모바일에서 멀티 토글 19 · 드롭다운 27 로 찌그러짐 — 세 컴포넌트에 모바일 크기가 정본에 없음 | **river 결정 대기** |

검증자가 함께 확인한 것: 0.5.3 배포본의 괄호 깨진 선택자 41줄 → 0.5.4 에서 0줄, 밀도 경로와 크기 지정 경로의 DOM·계산된 스타일 차이 0(16가지 상태 × 3밀도, 다크 포함), 밀도 밖 컴포넌트 오염 0.

검증자가 지적한 잠복 함정 3종(이중 부정 `:not(:not([data-size]))` · `:is()` 안 혼합 · `:where()` 로 묶은 크기)은 **검사기에서 차단**으로 넣었다. 현재 소스에는 해당 패턴 0건.

## 2차 독립 검증 (🤖 component-verifier · 델타 · 0.5.5) — **FAIL**

1차의 A·B·C 는 해소 확인(예시 마크업 103노드 0 차이 · 시트 137노드 0 차이 · 다중 인스턴스·재초기화·중첩 감싸개 전부 정상). 새로 2건이 드러났고 **둘 다 고쳤다**.

| # | 무엇이 | 조치 |
|---|---|---|
| D | 문서가 권하는 예외 처리(감싸기 안에서 `data-size` 만 주기)가 칩·필터칩·입력 3종에서 무너짐 — 그 셋은 크기 규칙이 `data-break` 를 함께 요구한다 | **고침** — 아래 '다리' |
| F | 모바일 감싸기만 주면 입력의 아이콘 버튼이 PC 크기로 남음(터치 영역 48→28). 검수 화면에 아이콘이 없어 캡처에도 안 잡혔다 | **고침** — 아래 '다리' |
| E | 밀도 선언을 겹치면 안쪽이 이기지 않는다(좁게 안의 넓게가 28) | **river 결정 대기** · 문서에 "겹치지 말라"로 적음 |

**다리(bridge)** — D·F 는 뿌리가 같다(컴포넌트 자기 `[data-break]` 규칙이 감싸기 선언만으로는 안 선다). 값은 건드리지 않고, 컴포넌트 자기 규칙을 화면 구분 조건만 느슨하게 해(`[data-break="pc"]` → `:not([data-break="mobile"])`) 감싸기 안에 그대로 깔아 준다. 반대쪽을 직접 적어 둔 컴포넌트는 여전히 비켜 간다.

함께 처리한 보강 3건: 검사기 사각지대(모바일 블록 13%)를 메웠고, 기본 높이 판정을 컨트롤 자리 하나로 좁혔으며(장식 부품 오인 차단), 코드 주석의 "반대쪽이 이긴다"를 실제 동작으로 정정했다. 문서에는 중첩 비지원과 "감싸기를 바꾸면 날짜 선택 재초기화" 두 줄을 넣었다.

## 3차 독립 검증 (🤖 component-verifier · 델타 · 0.5.6) — **FAIL**

D·F 와 보강 3건은 해소 확인(기존 마크업 2600노드 0 차이 · 다리가 밀도 밖 15종에 선택자 0개 · 규칙 1188/1188 그대로). **F 를 고치면서 난 퇴행 1건**이 새로 잡혔고, 고쳤다.

| # | 무엇이 | 조치 |
|---|---|---|
| G | 모바일을 감싸개로만 선언하면 입력의 지우기 아이콘에 PC hover 칠이 그대로 뜬다. 누르는 영역이 28→48 로 커지면서 river 가 2026-09-07 에 없애라고 한 바로 그 그림이 되돌아왔다 | **고침** — hover 억제 규칙 조건에 감싸개를 더했다(자기가 pc 라고 적었으면 그쪽이 이긴다). 값은 그대로 |
| — | 검사기 가드 면제를 줄 생김새로 가려서, 크기 치환을 빠뜨린 진짜 위반이 다리와 똑같이 생겨 함께 빠져나갔다 | **고침** — 블록 출처로 가린다. 가짜 위반 4종 적대 테스트 전부 차단 확인 |
| — | 이중 부정 함정이 `data-size` 에만 걸려 있었다 | **고침** — `:not(:not(` 일반형으로 |

새 검사 하나를 더 넣었다 — 밀도 대상 10종의 `@media` 안 규칙이 화면 구분을 보면서 감싸개 선언을 무시하면 **실패**시킨다. 이번 퇴행이 난 자리다.

**입력 지우기 버튼 실측** — 모바일 감싸개만: 48×48 · PC hover 칠 없음 / PC 계열: 28×28 · 칠 있음 / 모바일 감싸개 안에서 자기 `data-break="pc"`: 28×28 · 칠 있음(직접 지정이 이김). ✅

## 렌더 선캡처 (2026-09-15 09:01 KST · Chrome headless · DPR 2)

| 파일 | URL | 뷰포트 |
|---|---|---|
| `screens/pc-light.png` | `?theme=light` | 1280×1700 |
| `screens/pc-dark.png` | `?theme=dark` | 1280×1700 |
| `screens/mobile-light.png` | `?theme=light&only=m` | 390×900 |
| `screens/mobile-dark.png` | `?theme=dark&only=m` | 390×900 |
| `screens/mobile-own-break-light.png` | `?theme=light&only=b` | 390×900 |
| `screens/pc-explicit-size-light.png` | `?theme=light&only=s` | 1280×500 |

## 4차 독립 검증 (🤖 component-verifier · 델타 · 0.5.7) — **FAIL (사유는 결정 대기뿐)**

G 해소 확인. 새 선택자를 14가지 배치로 재봤고(감싸개 밖·형제 자리·중첩·자기 pc 선언) 전부 의도대로 골랐다. 이번 라운드가 만든 결함 **0건**.

대조 규모: 기존 마크업이 감싸개 안에서 달라지는지 **2600노드 0 차이**, 밀도 경로 ↔ 크기 직접 경로 **900노드 0 차이**, 실제 예시 마크업 **197노드 0 차이**, 규칙 파싱 **1188/1188**, 밀도 밖 컴포넌트 오염 **0**.

검사기 적대 테스트 9종 중 2종이 아직 샌다고 해서 그중 하나(다리 줄과 선택자가 같은 위반)는 **줄 번호 기준 면제로 고쳤고**, 나머지 하나(다리 블록 안에 숨긴 위반)는 지금 해당 컴포넌트가 없어 정리 과제로 남긴다.

## 최종 확인 (🤖 component-verifier · 0.5.7 무변경 · 2026-09-15) — **PASS**

river 가 남은 3건을 결정한 뒤의 확인. 배포본을 만드는 입력(`ui-library/src`·`scripts`·`plugins`·`registry/components`)이 전부 무변경이고 `ui:build:check` 가 exit 0 이라 **직전 검증 대상과 같은 배포본**임이 증명됐고, 그 위에서 결정 반영과 검사기 동작만 확인했다.

- 결정 3건이 정책 JSON·문서 §8·장부에 **발화 그대로** 실렸다.
- 검사기 3갈래가 실제로 작동한다 — 대체 지정을 지우면 "찌그러집니다"로 **실패**시킨다(가짜 정책으로 확인).
- 지난 라운드에 뚫렸던 "다리 줄과 선택자가 같은 위반 숨기기"도 이제 막힌다.
- 누적 ❌ 8건 전부 해소.

**검증자가 단 조건** — river 발화 3건이 실제 발화인지는 검증자가 확인할 수 없다. 최종 보고에서 river 에게 세 문장을 그대로 보여 주고 확인을 받아야 한다.

## river 결정 3건

| # | 물은 것 | river 발화 | 반영 |
|---|---|---|---|
| 1 | 좁게에서 멀티 토글·표가 34 로 올라가는 것 | "1번은 그대로 둘것" | `density-policy.json` 의 `fallbackDirection` |
| 2 | 모바일에서 멀티 토글 19 · 드롭다운 27 로 찌그러지는 것 | "모바일에서는 드롭다운 대신 바텀시트를 제공해. 멀티토글 대신 라디오/체크박스를 사용해." | `density-policy.json` 의 `mobileSubstitutes` · 검사기 3갈래 |
| 3 | 밀도 선언 겹쳐 쓰기를 지원할지 | "지금처럼 겹치지 마세요로 둬" | 문서 §8 문구 유지 |

## 남은 것

- **river 결정 3건** — 좁게에서 멀티 토글·표가 34 로 올라가는 것 · 모바일에서 멀티 토글·드롭다운이 찌그러지는 것 · 밀도 선언 겹치기를 지원할지
