'use strict';

const { sceneNodeDeclarations, renderGuideSceneNode } = require('./component-guide-scene-renderer');
const formGuide = require('./component-guide-form');

function esc(value) { return String(value).replace(/[&<>]/g, (c) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;' }[c])); }
function slug(value) { return String(value).trim().toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, ''); }
function cssVar(token) { return token && !/^\(|\?/.test(token) ? `var(--${token.replace(/\//g, '-')})` : 'transparent'; }
function setFor(model, name) { const set = model.componentSets.find((item) => item.name === name); if (!set) throw new Error(`component guide model에 ${name} 세트가 없습니다.`); return set; }
function findNode(node, predicate) { if (predicate(node)) return node; for (const child of node.children || []) { const hit = findNode(child, predicate); if (hit) return hit; } return null; }
function textNode(node) { return findNode(node, (item) => item.type === 'TEXT'); }
function px(value, fallback = 0) { return `${value == null ? fallback : value}px`; }
function style(node, includeWidth = true) { const text = textNode(node); return [sceneNodeDeclarations(node, { includeWidth, colorFor: cssVar }), text ? `color:${cssVar(text.appearance.fill)}` : null, text && text.text.fontSize ? `font-size:${px(text.text.fontSize)}` : null].filter(Boolean).join(';'); }
function coverage(axes) { const map = { Size: 'sizes', State: 'states', Type: 'type', Variant: 'variant', Align: 'align', state: 'states' }; return Object.entries(axes).map(([key, values]) => `data-cov-${map[key] || slug(key)}="${values.map(slug).join(',')}"`).join(' '); }

function dataFor(model) {
  const table = setFor(model, 'Table'), tableCell = setFor(model, 'Table Cell'), checkbox = setFor(model, 'Checkbox'), pagination = setFor(model, 'Pagination'), gnb = setFor(model, 'GNB'), gnbMenu = setFor(model, 'GNB Menu'), gnbUtility = setFor(model, 'GNB Utility Icon'), mobileBottomNav = setFor(model, 'Mobile Bottom Nav');
  if (table.variantCount !== 2 || tableCell.variantCount !== 8 || checkbox.variantCount !== 5 || pagination.variantCount !== 4 || gnb.variantCount !== 6 || gnbMenu.variantCount !== 9 || gnbUtility.variantCount !== 5 || mobileBottomNav.variantCount !== 2) throw new Error('Table/Navigation 정본 variant 개수가 검증 사실과 다릅니다.');
  return { table, tableCell, checkbox, pagination, gnb, gnbMenu, gnbUtility, mobileBottomNav };
}

function cssFor(data) {
  const tableRules = data.table.variants.map((v) => `    .guide-table[data-size="${slug(v.axes.Size)}"]{width:${px(v.node.dimensions.width)};max-width:100%;border-collapse:collapse}`).join('\n');
  const cellRules = data.tableCell.variants.map((v) => `    .guide-table-cell[data-size="${slug(v.axes.Size)}"][data-type="${slug(v.axes.Type)}"][data-variant="${slug(v.axes.Variant)}"]{${style(v.node, false)}}`).join('\n');
  const paginationRules = data.pagination.variants.map((v) => `    .guide-pagination[data-state="${slug(v.axes.State)}"]{min-height:${px(v.node.dimensions.height)}}`).join('\n');
  const gnbRules = data.gnb.variants.map((v) => `    .guide-gnb[data-align="${slug(v.axes.Align)}"][data-size="${slug(v.axes.Size)}"]{${style(v.node, false)};width:100%;display:flex;align-items:center;box-sizing:border-box}`).join('\n');
  const menuRules = data.gnbMenu.variants.map((v) => `    .guide-gnb-menu[data-size="${slug(v.axes.Size)}"][data-state="${slug(v.axes.State)}"]{${style(v.node)}}`).join('\n');
  const mobileRules = data.mobileBottomNav.variants.map((v) => `    .guide-mobile-tab[data-state="${slug(v.axes.state)}"]{${style(v.node)};display:inline-flex;flex-direction:column;align-items:center;justify-content:center;gap:${px(v.node.layout.gap)}}`).join('\n');
  const tableAliases = data.table.variants.map((v) => `    .s1-table--${slug(v.axes.Size)}{max-width:100%;height:${px(v.node.dimensions.height)}}`).join('\n');
  const gnbAliases = data.gnbMenu.axes.Size.map((size) => `    .s1-gnb-menu--${slug(size)}{height:${px(data.gnbMenu.variants.find((v) => v.axes.Size === size).node.dimensions.height)}}`).join('\n');
  return `    .main-content,.page-content{min-width:0;box-sizing:border-box}.main-content{max-width:100%}.page-content{max-width:none}.comp-section,.comp-section>.variant-block,.comp-section .preview-area,.comp-section .code-block,.comp-section .code-pane{min-width:0;max-width:100%;box-sizing:border-box}.comp-section .code-block pre{display:block;max-width:100%;overflow:auto;white-space:pre}.guide-table-wrap{position:relative;max-width:100%;overflow:auto}.guide-table th,.guide-table td{position:relative;box-sizing:border-box;text-align:left}.guide-table-cell-divider{position:absolute;left:0;right:0;bottom:0;display:block;pointer-events:none}.guide-table-edge{position:absolute;left:0;z-index:2;display:block;pointer-events:none}.guide-table-edge--top{top:0}.guide-table-edge--bottom{bottom:0}.guide-table-footer-cell{padding:0}.guide-table-footer{display:flex;align-items:center;justify-content:center;position:relative}.guide-table-footer .guide-select{position:absolute;right:0}.guide-table th[aria-sort]{position:relative}.guide-table [data-table-sort]{all:unset;position:absolute;inset:0;display:flex;align-items:center;box-sizing:border-box;padding:inherit;color:inherit;font:inherit;cursor:pointer}.guide-table [data-table-sort]:focus-visible{outline:2px solid var(--color-action-primary-default);outline-offset:-2px}.guide-pagination{display:inline-flex;align-items:center;gap:var(--spacing-4)}.guide-pagination button{font:inherit}.guide-pagination [aria-current="page"]{font-weight:var(--font-weight-bold)}.guide-gnb{justify-content:space-between;overflow:hidden}.guide-gnb[data-align="start"]{justify-content:flex-start}.guide-gnb-menus{display:flex;align-items:stretch;min-width:0}.guide-gnb-menu{box-sizing:border-box;border:0;background:transparent;font-family:inherit}.guide-gnb-utility{display:flex;align-items:center;margin-left:auto}.guide-gnb-utility-action{all:unset;display:inline-flex;align-items:center;justify-content:center;cursor:pointer}.guide-mobile-tablist{display:inline-flex}.guide-mobile-tab{box-sizing:border-box;border:0;background:transparent;font-family:inherit}.guide-preview-static{pointer-events:none}
${tableAliases}\n${gnbAliases}
${tableRules}\n${cellRules}\n${paginationRules}\n${gnbRules}\n${menuRules}\n${mobileRules}`;
}

function tokenRows(set) { const tokens = new Set(); const walk = (node) => { for (const token of [node.appearance.fill, node.appearance.stroke]) if (token && !token.startsWith('(')) tokens.add(token); for (const child of node.children || []) walk(child); }; set.variants.forEach((v) => walk(v.node)); return [...tokens].map((token) => `<tr><td class="token-name">--${esc(token.replace(/\//g, '-'))}</td><td class="token-value">${esc(token)}</td><td>정본 바인딩</td></tr>`).join(''); }
function code(prefix, markup, css, sets) { return `<div class="code-block"><div class="code-tabs"><button class="code-tab active" onclick="switchTab(this,'${prefix}-html')">HTML</button><button class="code-tab" onclick="switchTab(this,'${prefix}-css')">CSS</button><button class="code-tab" onclick="switchTab(this,'${prefix}-tokens')">Token Details</button><button class="code-copy-btn" onclick="copyCode('${prefix}-html-pre')">복사</button></div><div class="code-pane active" id="${prefix}-html"><pre id="${prefix}-html-pre">${esc(markup)}</pre></div><div class="code-pane" id="${prefix}-css"><pre>${esc(css)}</pre></div><div class="code-pane" id="${prefix}-tokens"><table class="token-detail-table"><tbody>${sets.map(tokenRows).join('')}</tbody></table></div></div>`; }
function header(name, count, description) { return `<div class="comp-section-header"><h2 class="comp-section-title">${esc(name)}</h2><span class="comp-badge comp-badge-blue">Core</span><span class="comp-badge">${count} variants</span><span class="comp-badge">interactive</span></div><p class="variant-group-desc">${esc(description)}</p>`; }

function checkbox(data, label, checked = 'false', preview = false) { const state = checked === 'false' ? 'Default' : 'Checked'; if (!data.checkbox.variants.some((variant) => variant.axes.State === state)) throw new Error(`Table Checkbox 정본 상태가 없습니다: ${state}`); return `<button type="button" class="guide-selection-control guide-checkbox${preview ? ' is-preview' : ''}" data-component="checkbox" data-component-context="table" data-state="${slug(state)}" role="checkbox" aria-checked="${checked}" aria-label="${esc(label)}"><span class="guide-checkbox-box"><span class="guide-checkbox-mark" aria-hidden="true">${checked === 'mixed' ? '−' : '✓'}</span></span></button>`; }
function divider(node) { const line = (node.children || []).find((child) => child.type === 'RECTANGLE'); return line ? `<span class="guide-table-cell-divider" aria-hidden="true" style="height:${px(line.dimensions.height)};background:${cssVar(line.appearance.fill)}"></span>` : ''; }
function tableCell(node, content, attrs = '') { return `<td class="guide-table-cell" ${attrs} style="width:${px(node.dimensions.width)};height:${px(node.dimensions.height)};${node.appearance.fill && !/^\(|\?/.test(node.appearance.fill) ? `background:${cssVar(node.appearance.fill)}` : ''}">${content}${divider(node)}</td>`; }
function tableMarkup(data, size, meta, preview = false, deps = {}) {
  const variant = data.table.variants.find((item) => item.axes.Size === size); if (!variant) throw new Error(`Table ${size} 정본 variant가 없습니다.`);
  const scene = variant.node, headerScene = scene.children.find((child) => child.name === 'header'), rowScenes = scene.children.filter((child) => /^row-\d+$/.test(child.name)), footerScene = scene.children.find((child) => child.name === 'table-footer'), top = scene.children.find((child) => child.name === 'edge-top'), bottom = scene.children.find((child) => child.name === 'edge-bottom');
  if (!headerScene || rowScenes.length !== 8 || !footerScene || !top || !bottom) throw new Error(`Table ${size} scene graph의 8행/footer/strong line 계약이 다릅니다.`);
  if (meta.guide.sampleRows.length !== rowScenes.length || meta.guide.sampleColumns.length !== headerScene.children.length) throw new Error('Table Registry 예시 데이터가 정본 열/행 수와 다릅니다.');
  const headerCells = headerScene.children.map((cell, index) => index === 0
    ? `<th class="guide-table-cell" scope="col" style="width:${px(cell.dimensions.width)};height:${px(cell.dimensions.height)}">${checkbox(data, '전체 행 선택', 'mixed', preview)}${divider(cell)}</th>`
    : `<th class="guide-table-cell" scope="col"${index === 1 ? ' aria-sort="none"' : ''} style="width:${px(cell.dimensions.width)};height:${px(cell.dimensions.height)}">${index === 1 ? `<button type="button" data-table-sort>${esc(meta.guide.sampleColumns[index])}</button>` : esc(meta.guide.sampleColumns[index])}${divider(cell)}</th>`).join('');
  const body = rowScenes.map((rowScene, rowIndex) => `<tr data-table-body-row="${rowIndex + 1}">${rowScene.children.map((cell, colIndex) => colIndex === 0
    ? tableCell(cell, checkbox(data, `${rowIndex + 1}행 선택`, rowIndex === 2 ? 'true' : 'false', preview))
    : tableCell(cell, esc(meta.guide.sampleRows[rowIndex][colIndex - 1]))).join('')}</tr>`).join('');
  let footer = '';
  if (deps.form && deps.selectMeta && deps.dropdownMeta && deps.paginationMeta) {
    const select = deps.form.select.variants.find((item) => item.axes.Break === 'PC' && item.axes.Size === 'XXSM' && item.axes.State === 'Default');
    footer = `<tfoot><tr><td class="guide-table-footer-cell" colspan="${headerScene.children.length}"><div class="guide-table-footer" style="height:${px(footerScene.dimensions.height)};background:${cssVar(footerScene.appearance.fill)}">${paginationMarkup('Middle', deps.paginationMeta, preview)}${formGuide.selectMarkup(select, deps.selectMeta, deps.dropdownMeta, preview, deps.form.dropdown)}</div></td></tr></tfoot>`;
  }
  return `<div class="guide-table-wrap" style="width:${px(scene.dimensions.width)}"><span class="guide-table-edge guide-table-edge--top" data-table-strong-line="top" aria-hidden="true" style="width:${px(top.dimensions.width)};height:${px(top.dimensions.height)};background:${cssVar(top.appearance.fill)}"></span><table class="guide-table s1-table--${slug(size)}${preview ? ' guide-preview-static' : ''}" data-component="table" data-size="${slug(size)}"><thead><tr>${headerCells}</tr></thead><tbody>${body}</tbody>${footer}</table><span class="guide-table-edge guide-table-edge--bottom" data-table-strong-line="bottom" aria-hidden="true" style="width:${px(bottom.dimensions.width)};height:${px(bottom.dimensions.height)};background:${cssVar(bottom.appearance.fill)}"></span></div>`;
}
function tableSection(data, meta, css, deps) { const cells = data.tableCell.variants.map((v) => `<div class="comp-state-cell"><div class="matrix-row-label">${esc(v.name)}</div><div class="guide-table-cell" data-size="${slug(v.axes.Size)}" data-type="${slug(v.axes.Type)}" data-variant="${slug(v.axes.Variant)}">셀 예시${divider(v.node)}</div></div>`).join(''); const examples = data.table.axes.Size.map((size) => tableMarkup(data, size, meta, false, deps)).join('\n'); const dependencies = meta.guide.dependencies.join(','); return `<section class="comp-section" id="table" ${coverage(data.table.axes)} ${coverage(data.tableCell.axes)} data-dependencies="${esc(dependencies)}">${header(meta._meta.name, data.table.variantCount, meta._meta.description)}<p class="variant-group-desc">정본 의존성: ${esc(meta.guide.dependencies.join(' · '))}</p><div class="variant-block"><div class="variant-label">Action · sortable/selectable table</div><div class="preview-area">${tableMarkup(data, 'MD', meta, false, deps)}</div></div><div class="variant-block"><div class="variant-label">Table variants</div><div class="preview-area">${data.table.axes.Size.map((size) => tableMarkup(data, size, meta, true, deps)).join('')}</div></div><div class="variant-block"><div class="variant-label">Table Cell variants</div><div class="preview-area" style="display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));gap:12px">${cells}</div></div>${code('table', examples, css, [data.table, data.tableCell, data.checkbox, data.pagination])}</section>`; }

function paginationMarkup(state, meta, preview = false) { const pages = state === 'Single' ? [meta.guide.samplePages[0]] : meta.guide.samplePages; const current = state === 'Last' ? pages.length - 1 : state === 'Middle' ? Math.floor(pages.length / 2) : 0; const disabledStart = state === 'Single' || state === 'First', disabledEnd = state === 'Single' || state === 'Last'; return `<nav class="guide-pagination${preview ? ' guide-preview-static' : ''}" data-component="pagination" data-state="${slug(state)}" aria-label="페이지 탐색"><button type="button" data-page-action="first" aria-label="${esc(meta.guide.labels.first)}"${disabledStart ? ' disabled' : ''}>«</button><button type="button" data-page-action="previous" aria-label="${esc(meta.guide.labels.previous)}"${disabledStart ? ' disabled' : ''}>‹</button>${pages.map((page, index) => `<button type="button" data-page-number="${index + 1}"${index === current ? ' aria-current="page"' : ''}>${esc(page)}</button>`).join('')}<button type="button" data-page-action="next" aria-label="${esc(meta.guide.labels.next)}"${disabledEnd ? ' disabled' : ''}>›</button><button type="button" data-page-action="last" aria-label="${esc(meta.guide.labels.last)}"${disabledEnd ? ' disabled' : ''}>»</button></nav>`; }
function paginationSection(data, meta, css) { const variants = data.pagination.variants.map((v) => `<div class="comp-state-cell"><div class="matrix-row-label">${esc(v.name)}</div>${paginationMarkup(v.axes.State, meta, true)}</div>`).join(''); return `<section class="comp-section" id="pagination" ${coverage(data.pagination.axes)}>${header(meta._meta.name, data.pagination.variantCount, meta._meta.description)}<div class="variant-block"><div class="variant-label">Action · dynamic current page</div><div class="preview-area">${paginationMarkup('Middle', meta)}</div></div><div class="variant-block"><div class="variant-label">Canonical variants</div><div class="preview-area" style="display:grid;gap:12px">${variants}</div></div>${code('pagination', data.pagination.axes.State.map((state) => paginationMarkup(state, meta)).join('\n'), css, [data.pagination])}</section>`; }

function gnbMenu(size, state, label, preview = false) { return `<button type="button" class="guide-gnb-menu s1-gnb-menu${size !== 'md' ? ` s1-gnb-menu--${slug(size)}` : ''}${state === 'Selected' ? ' is-selected' : ''}${preview ? ' guide-preview-static' : ''}" data-component="gnb-menu" data-size="${slug(size)}" data-state="${slug(state)}"${state === 'Selected' ? ' aria-current="page"' : ''}>${esc(label)}</button>`; }
function gnbUtility(node, meta) { const labels = meta.guide.utilityLabels; if (!labels) throw new Error('GNB Registry utilityLabels가 없습니다.'); const children = node.children || []; if (children.length !== 3) throw new Error('GNB Utility Icon 정본의 3개 instance 영역이 없습니다.'); return `<div class="guide-gnb-utility" data-component-dependency="gnb-utility-icon">${children.map((child, index) => { const role = index === 0 ? 'language' : index === 1 ? 'account' : 'menu'; return `<button type="button" class="guide-gnb-utility-action" data-utility-role="${role}" aria-label="${esc(labels[role])}">${renderGuideSceneNode(child)}</button>`; }).join('')}</div>`; }
function gnbMarkup(data, axes, meta, preview = false) { const variant = data.gnb.variants.find((item) => item.axes.Align === axes.Align && item.axes.Size === axes.Size); if (!variant) throw new Error(`GNB 정본 variant가 없습니다: ${JSON.stringify(axes)}`); const utility = variant.node.children.find((child) => /^language=/.test(child.name)); if (!utility) throw new Error(`GNB ${variant.name} Utility Icon instance가 없습니다.`); return `<header class="guide-gnb${preview ? ' guide-preview-static' : ''}" data-component="gnb" data-align="${slug(axes.Align)}" data-size="${slug(axes.Size)}"><strong>${esc(meta.guide.sampleLogo)}</strong><div class="guide-gnb-menus">${meta.guide.sampleMenus.map((label, index) => gnbMenu(axes.Size, index === 0 ? 'Selected' : 'Default', label, preview)).join('')}</div>${gnbUtility(utility, meta)}</header>`; }
function gnbSection(data, meta, css) { const bars = data.gnb.variants.map((v) => `<div class="comp-state-cell"><div class="matrix-row-label">${esc(v.name)}</div>${gnbMarkup(data, v.axes, meta, true)}</div>`).join(''); const menus = data.gnbMenu.variants.map((v) => `<div class="comp-state-cell"><div class="matrix-row-label">${esc(v.name)}</div>${gnbMenu(v.axes.Size, v.axes.State, meta.guide.sampleMenus[0], true)}</div>`).join(''); return `<section class="comp-section" id="gnb" ${coverage(data.gnb.axes)} data-cov-states="${data.gnbMenu.axes.State.map(slug).join(',')}">${header(meta._meta.name, data.gnb.variantCount, meta._meta.description)}<div class="variant-block"><div class="variant-label">Action · current menu</div><div class="preview-area">${gnbMarkup(data, { Align: 'Center-Between', Size: 'md' }, meta)}</div></div><div class="variant-block"><div class="variant-label">GNB variants</div><div class="preview-area" style="display:grid;gap:12px">${bars}</div></div><div class="variant-block"><div class="variant-label">Internal GNB Menu variants</div><div class="preview-area" style="display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));gap:12px">${menus}</div></div>${code('gnb-pc', data.gnb.variants.map((v) => gnbMarkup(data, v.axes, meta)).join('\n'), css, [data.gnb, data.gnbMenu, data.gnbUtility])}</section>`; }

function mobileTab(data, state, label, preview = false) { const selected = state === 'selected', variant = data.mobileBottomNav.variants.find((item) => item.axes.state === state); const icon = findNode(variant.node, (item) => item.asset && item.asset.kind === 'svg'); if (!icon) throw new Error(`Mobile Bottom Nav ${state} home icon이 없습니다.`); return `<button type="button" class="guide-mobile-tab${preview ? ' guide-preview-static' : ''}" data-component="mobile-bottom-nav-item" data-state="${state}" role="tab" aria-selected="${selected}" tabindex="${selected ? 0 : -1}">${renderGuideSceneNode(icon, { className: 'guide-mobile-tab-icon' })}<span>${esc(label)}</span></button>`; }
function mobileGroup(data, meta, preview = false) { return `<div class="guide-mobile-tablist${preview ? ' guide-preview-static' : ''}" data-component="mobile-bottom-nav" role="tablist" aria-label="하단 탭">${meta.guide.sampleLabels.map((label, index) => mobileTab(data, index === 0 ? 'selected' : 'unselected', label, preview)).join('')}</div>`; }
function mobileSection(data, meta, css) { const items = data.mobileBottomNav.variants.map((v) => `<div class="comp-state-cell"><div class="matrix-row-label">${esc(v.name)}</div>${mobileTab(data, v.axes.state, meta.guide.sampleLabels[0], true)}</div>`).join(''); return `<section class="comp-section" id="mobile-bottom-nav" ${coverage(data.mobileBottomNav.axes)}>${header(meta._meta.name, data.mobileBottomNav.variantCount, meta._meta.description)}<p class="variant-group-desc">${esc(meta.guide.boundaryNote)}</p><div class="variant-block"><div class="variant-label">Action · Tab Item 최소 조합</div><div class="preview-area">${mobileGroup(data, meta)}</div></div><div class="variant-block"><div class="variant-label">Tab Item canonical variants</div><div class="preview-area" style="display:flex;gap:12px">${items}</div></div>${code('bottom-nav', mobileGroup(data, meta), css, [data.mobileBottomNav])}</section>`; }

function runtime() { return `<script data-component-guide-table-navigation-runtime>;(()=>{if(window.__componentGuideTableNavigationReady)return;window.__componentGuideTableNavigationReady=true;function pageButtons(root){return Array.from(root.querySelectorAll('[data-page-number]'))}function updatePagination(root,index){const pages=pageButtons(root);if(!pages.length)return;const next=Math.max(0,Math.min(index,pages.length-1));pages.forEach((page,i)=>{if(i===next)page.setAttribute('aria-current','page');else page.removeAttribute('aria-current')});root.querySelectorAll('[data-page-action="first"],[data-page-action="previous"]').forEach(button=>button.disabled=next===0);root.querySelectorAll('[data-page-action="next"],[data-page-action="last"]').forEach(button=>button.disabled=next===pages.length-1);root.dataset.currentPage=String(next+1)}function setTableCheck(check,value){check.setAttribute('aria-checked',String(value));check.dataset.state=value===true?'checked':'default';const mark=check.querySelector('.guide-checkbox-mark');if(mark)mark.textContent=value==='mixed'?'−':'✓'}function syncTableHeader(root){const rows=Array.from(root.querySelectorAll('tbody [role="checkbox"]')),header=root.querySelector('thead [role="checkbox"]'),count=rows.filter(check=>check.getAttribute('aria-checked')==='true').length;if(header)setTableCheck(header,count===0?false:count===rows.length?true:'mixed')}document.querySelectorAll('[data-component="pagination"]').forEach(root=>{const pages=pageButtons(root),current=pages.findIndex(page=>page.getAttribute('aria-current')==='page');updatePagination(root,current<0?0:current)});document.addEventListener('click',e=>{const sort=e.target.closest('[data-table-sort]');if(sort){const th=sort.closest('th'),now=th.getAttribute('aria-sort');th.setAttribute('aria-sort',now==='ascending'?'descending':'ascending');return}const check=e.target.closest('[data-component="table"] [role="checkbox"]');if(check&&!check.closest('.guide-preview-static')){const root=check.closest('[data-component="table"]'),on=check.getAttribute('aria-checked')!=='true';if(check.closest('thead'))root.querySelectorAll('tbody [role="checkbox"]').forEach(row=>setTableCheck(row,on));else setTableCheck(check,on);syncTableHeader(root);return}const page=e.target.closest('[data-component="pagination"] [data-page-number],[data-component="pagination"] [data-page-action]');if(page&&!page.closest('.guide-preview-static')){const root=page.closest('[data-component="pagination"]'),pages=pageButtons(root),current=Math.max(0,pages.findIndex(item=>item.getAttribute('aria-current')==='page'));let next=page.hasAttribute('data-page-number')?pages.indexOf(page):current;const action=page.dataset.pageAction;if(action==='first')next=0;else if(action==='previous')next=current-1;else if(action==='next')next=current+1;else if(action==='last')next=pages.length-1;updatePagination(root,next);return}const menu=e.target.closest('[data-component="gnb"] [data-component="gnb-menu"]');if(menu&&!menu.closest('.guide-preview-static')){menu.parentElement.querySelectorAll('[data-component="gnb-menu"]').forEach(x=>{x.removeAttribute('aria-current');x.dataset.state='default'});menu.setAttribute('aria-current','page');menu.dataset.state='selected';return}const tab=e.target.closest('[data-component="mobile-bottom-nav"] [role="tab"]');if(tab&&!tab.closest('.guide-preview-static'))selectTab(tab.closest('[data-component="mobile-bottom-nav"]'),tab,true)});function tabs(root){return Array.from(root.querySelectorAll('[role="tab"]'))}function selectTab(root,item,focus){tabs(root).forEach(tab=>{const on=tab===item;tab.setAttribute('aria-selected',String(on));tab.tabIndex=on?0:-1;tab.dataset.state=on?'selected':'unselected'});if(focus)item.focus()}document.addEventListener('keydown',e=>{const tab=e.target.closest('[data-component="mobile-bottom-nav"] [role="tab"]');if(!tab)return;const root=tab.closest('[data-component="mobile-bottom-nav"]'),list=tabs(root),i=list.indexOf(tab);let n=i;if(e.key==='ArrowRight')n=(i+1)%list.length;else if(e.key==='ArrowLeft')n=(i-1+list.length)%list.length;else if(e.key==='Home')n=0;else if(e.key==='End')n=list.length-1;else return;e.preventDefault();selectTab(root,list[n],true)})})();</script>`; }

module.exports = { dataFor, cssFor, tableSection, paginationSection, gnbSection, mobileSection, runtime };
