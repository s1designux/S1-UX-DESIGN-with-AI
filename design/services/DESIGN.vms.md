---
version: 1.0.0
name: S1 Design System — VMS
scope: vms
extends: ../DESIGN.core.md
description: VMS 서비스 분기(core 를 상속, 차이분만 기재 · 자동 생성)
---

# S1 Design System — VMS

> ⚠️ 자동 생성물. 손편집 금지. 이 파일은 `../DESIGN.core.md` 를 상속하며, 아래는 이 서비스로 분기된 컴포넌트만 나열합니다.

## Components

_아직 `scope="vms"` 로 분기된 컴포넌트가 없습니다. core 정의를 그대로 상속합니다._

컴포넌트를 이 서비스로 분기하려면 해당 `registry/components/*.json` 의 `_meta.scope` 를 `"vms"` 로 설정한 뒤 `npm run design:md:write` 를 실행하세요.

<!-- generated-stamp: f3faf363a923 · 손편집 금지 -->
